/** Public Google Maps listing — used for "see all reviews" and future sync. */
export const GOOGLE_MAPS_REVIEWS_URL =
  'https://maps.app.goo.gl/kuZ5tKfGBwEjtk95A';

/** Optional override when VITE_REVIEWS_API_URL is unset (production uses /api/google-reviews). */
export const GOOGLE_REVIEWS_API_PATH = '/api/google-reviews';
